#!/usr/bin/env node
import{existsSync,readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';
const root=resolve(join(dirname(fileURLToPath(import.meta.url)),'..','..'));const required=['AGENTS.md','CLAUDE.md','AGENT_HARNESS.md','.claude/settings.json','.claude/hooks/pm-rules.sh','.claude/hooks/runtime-guard.mjs','.claude/hooks/workspace-guard.mjs','.claude/memory/MEMORY.md','.claude/memory/project-overview.md','.codex/config.toml','.codex/hooks/pm-rules.ps1','.codex/memory/PROJECT_MEMORY.md','.codex/memory/AGENT_HANDOFF.md'];
try{const warnings=[];const missing=required.filter((file)=>!existsSync(join(root,file)));if(missing.length)warnings.push(`missing: ${missing.join(', ')}`);const overview=readFileSync(join(root,'.claude/memory/project-overview.md'),'utf8');const verified=overview.match(/^last_verified:\s*(\d{4}-\d{2}-\d{2})\s*$/m)?.[1];if(!verified)warnings.push('project memory has no last_verified date');else{const age=Math.floor((Date.now()-Date.parse(`${verified}T00:00:00Z`))/86400000);if(age>90)warnings.push(`project memory is ${age} days old`);}if(warnings.length)console.log(`Agent harness warning: ${warnings.join('; ')}`);}catch{}
